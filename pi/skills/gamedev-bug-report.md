---
name: gamedev-bug-report
description: "Creates a structured bug report as a Backlog task (bug label), or analyzes code to identify potential bugs. Ensures every bug has full reproduction steps, severity assessment, and context. Verify and close modes drive the task's status through Backlog."
---

# Game Studio: bug-report

Read [the host guide](../../docs/host-runtime.md), then read and follow
[the shared workflow](../../skills/bug-report/SKILL.md).

Resolve the workflow's relative paths from its own directory, not this entry point. Use the text after this command as
the workflow's arguments.
