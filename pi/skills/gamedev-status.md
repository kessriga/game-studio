---
name: gamedev-status
description: "Print the current production stage and Epic > Feature > Task breadcrumb on demand. Use when the user asks what stage are we in, where are we, or show status."
---

# Game Studio: status

Read [the host guide](../../docs/host-runtime.md), then read and follow
[the shared workflow](../../skills/status/SKILL.md).

Resolve the workflow's relative paths from its own directory, not this entry point. Use the text after this command as
the workflow's arguments.
