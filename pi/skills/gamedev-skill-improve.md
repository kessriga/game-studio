---
name: gamedev-skill-improve
description: "Improve a skill using a test-fix-retest loop. Runs static checks, proposes targeted fixes, rewrites the skill, re-tests, and keeps or reverts based on score change."
---

# Game Studio: skill-improve

Read [the host guide](../../docs/host-runtime.md), then read and follow
[the shared workflow](../../skills/skill-improve/SKILL.md).

Resolve the workflow's relative paths from its own directory, not this entry point. Use the text after this command as
the workflow's arguments.
